# Required — LLM + embeddings
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# Required in production — persistent rate limiting (Upstash Redis)
# App will crash on startup without these in production
UPSTASH_REDIS_REST_URL=https://...upstash.io
UPSTASH_REDIS_REST_TOKEN=AX...
